DATA:
  DATA_NAME: 'ENIGMA'
  METRICS: "pAP"
EVALUATION:
  METHOD: "perpoint"
INPUT:
  MODALITY: 'visual'
  VISUAL_FEATURE: 'DINOv2'
  MOTION_FEATURE: 'DINOv2' #! PLACE HOLDER, NOT USED DURING FORWARD
  OBJECT_FEATURE: 'DINOv2' #! PLACE HOLDER, NOT USED DURING FORWARD
  TARGET_PERFRAME: 'target_end_perframe'
MODEL:
  MODEL_NAME: 'LSTR'
  FEATURE_HEAD:
    LINEAR_ENABLED: True
    LINEAR_OUT_FEATURES: 1024
  LSTR:
    MAMBA_LAYER: 0
    NUM_HEADS: 16
    DIM_FEEDFORWARD: 1024
    DROPOUT: 0.2
    ACTIVATION: 'relu'
    LONG_MEMORY_SECONDS: 0
    LONG_MEMORY_SAMPLE_RATE: 1
    LONG_MEMORY_USE_PE: False
    LONG_MEMORY_INCLUDE_WORK: False
    WORK_MEMORY_SECONDS: 1
    WORK_MEMORY_SAMPLE_RATE: 1
    ENC_MODULE: [
      [16, 1, True], [32, 2, True]
    ]
    DEC_MODULE: [-1, 2, True]
  CRITERIONS: [['MCE', {}]]
  FRAME_DELTA: 0
DATA_LOADER:
  BATCH_SIZE: 16
  NUM_WORKERS: 8
  PIN_MEMORY: True
SOLVER:
  NUM_EPOCHS: 50
  OPTIMIZER: 'adamw'
  BASE_LR: 7e-05
  WEIGHT_DECAY: 5e-05
  SCHEDULER:
    SCHEDULER_NAME: 'warmup_cosine'
    WARMUP_FACTOR: 0.3
    WARMUP_EPOCHS: 5.0
    WARMUP_METHOD: 'linear'
OUTPUT_DIR: 'ckpt'
